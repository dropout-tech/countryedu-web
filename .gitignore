# ─────────────────────────────────────────────────────────────
# countryedu-web — monorepo 根目錄 .gitignore
#
# 採「白名單」策略：此工作目錄常有並行 session 同步進來的
# 商務／法務／個資文件（會議記錄、契約、sales kit、課程設計…）。
# 為避免敏感資料誤上傳，預設忽略根目錄一切，只放行明確要版控的項目。
# 新增要版控的頂層項目時，請在下方白名單補一條 !/<name>。
# ─────────────────────────────────────────────────────────────

# 預設忽略根目錄下所有項目
/*

# 僅放行以下要版控的項目（敏感商務檔不在此列 → 自動排除）
!/web/
!/.github/
!/design/
!/README.md
!/.gitignore

# ── 即使在已放行的目錄內，仍一律排除以下 ──
# 相依套件 / 建置產物
node_modules/
.next/
out/
build/
*.tsbuildinfo

# Vercel
.vercel

# 環境變數
.env
.env*.local

# 編輯器 / 代理本機設定
.claude/
.vscode/
.idea/

# macOS
.DS_Store
